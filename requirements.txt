# Python dependencies for FlatCAM (Python 3.10–3.13)
# Install with: pip install -r requirements.txt

# GUI
PySide6>=6.6

# Scientific / geometry
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
shapely>=2.0
rtree>=1.1

# Utilities
simplejson>=3.19
svg.path>=6.3

# Windows-only: used for AppData path resolution in FlatCAMApp
# pywin32>=306; sys_platform == "win32"
